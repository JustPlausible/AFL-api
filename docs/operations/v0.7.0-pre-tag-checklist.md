# v0.7.0 pre-tag checklist

Record the exact candidate SHA and attach evidence for every checked item.

## Repository and release validation

- [ ] All intended v0.7.0 work, including the canonical roster implementation,
  is merged into the candidate; no unreviewed working-tree changes remain.
- [ ] The final release-readiness review has been refreshed as a distinct,
  dated assessment against the exact candidate SHA, with no known blocker.
- [ ] A representative supported pre-v0.7 database migrated successfully from
  head `0015` through v0.7.0 head `0024`; checksums, `integrity_check`, and
  `foreign_key_check` passed.
- [ ] The full automated suite and focused version/migration tests pass without
  requiring live AFL/CFS access.
- [ ] `python cli.py --version`, FastAPI/OpenAPI metadata, authenticated
  `/api/v1` discovery, and Scheduler health/version metadata all report
  `0.7.0`.
- [ ] Representative existing and new `/api/v1` resource smoke tests confirm
  ordinary reads are authentication-only and advanced reads enforce the
  `advanced-read` capability.
- [ ] README and documentation indexes identify v0.7.0; links and documented
  commands were checked.
- [ ] Release notes accurately describe the `292a5ce..CANDIDATE_SHA` delta,
  migration head, compatibility boundary, and current limitations.
- [ ] The tag name and annotation are prepared, but `v0.7.0` has not been
  created or pushed before all gates pass.

## Site-specific deployment actions

- [ ] Writer drain/stop was rehearsed for the target topology.
- [ ] A WAL-safe pre-migration backup was created, integrity-checked,
  foreign-key-checked, checksummed, and retained outside the live failure
  domain; restore was verified.
- [ ] The previous immutable image/digest and database restore procedure are
  retained and the rollback decision boundary is understood.
- [ ] One immutable image was built from the candidate SHA and its revision and
  version labels/digest were recorded for API, Admin, and Scheduler.
- [ ] Season bootstrap → sync → read-only report completed where required, and
  completeness/reconciliation findings are acceptable for the site's selected
  season.
- [ ] Required API keys are active with least-privilege capabilities; no secret
  values were copied into release evidence.
- [ ] API and Admin became ready before the single Scheduler was started;
  health, Scheduler diagnostics/logs, analytics, and deliberately enabled
  evidence profiles were reviewed.
- [ ] Representative populated-season v1 smoke tests passed from an authorized
  path, including discovery, navigation, player stats, and applicable player,
  injury, commentary, interchange, and roster resources.
